# Pitch Deck Grader - .env.example
# Copy to .env and customize values.

# ========================= Required =========================
# OpenAI API key used for grading LLM calls.
OPENAI_API_KEY=sk-your-api-key-here

# ========================= Optional =========================
# Override default model (service falls back if omitted).
# Examples: gpt-4o-mini, gpt-4o, gpt-4.1-mini
OPENAI_MODEL=gpt-4o-mini

# Port the Express server listens on (default 8080).
PORT=8080

# Logging verbosity: silent | error | warn | info | debug
LOG_LEVEL=info

# Max accepted file size in MB (enforced before LLM call, advisory if not wired yet).
MAX_FILE_SIZE_MB=25

# Comma-separated list of allowed CORS origins for production.
# Leave blank or omit for permissive local development.
ALLOWED_ORIGINS=http://localhost:3000

# Request timeout (ms) for LLM calls before fallback.
REQUEST_TIMEOUT_MS=60000

# Seed for deterministic scoring attempts (if model supports it).
LLM_SEED=42

# ========================= Future =========================
# (Uncomment when implemented)
# PERSISTENCE_DATABASE_URL=postgresql://user:pass@localhost:5432/capsight
# REDIS_URL=redis://localhost:6379
# RATE_LIMIT_PER_MINUTE=60

# ===========================================================
# After editing, restart the service:
#   npm run dev
# ===========================================================
